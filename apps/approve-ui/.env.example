# Optional. Identity UI defaults to the Vite `/id-api` proxy → http://127.0.0.1:8788
# VITE_IDENTITY_LOOP_BASE=http://127.0.0.1:8788
# UI-only smoke without identity-loop (also: /identity?mock=1)
# VITE_IDENTITY_MOCK=1
